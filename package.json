{
    "name": "smartOffice_anedya",
    "description": "Project : Smart office anedya",
    "version": "0.0.1",
    "dependencies": {
        "node-red": "^2.0.0" // Use the version that matches your local setup
    },
    "scripts": {
        "start": "node index.js"
    },
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}